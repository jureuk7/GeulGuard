# Contributing to GeulGuard

GeulGuard에 관심을 가져 주셔서 감사합니다.

## 기여 시작하기

버그 수정, 기능 개선, 테스트와 문서 기여를 환영합니다. 기존 이슈와 PR을 확인해
중복 작업을 줄여 주세요. 큰 변경은 구현 전에 이슈에서 방향을 논의해 주세요.
오타와 작은 수정은 이슈 없이 PR을 제출해도 됩니다.

쓰기 권한이 없으면 fork에서 작업한 뒤 원본 저장소의 `main`으로 PR을 보내 주세요.
자신의 fork를 `origin`, 원본 저장소를 `upstream`으로 등록해 동기화할 수 있습니다.

## 버그 제보

다음 정보를 GitHub Issue에 포함해 주세요.

- macOS 버전과 빌드 번호
- 문제가 발생한 앱과 버전
- 사용한 키 입력과 실제/기대 결과
- 가능하면 화면 녹화와 재현 빈도

키 입력에 개인정보가 포함되지 않도록 반드시 확인해 주세요.

## 개발 환경

- macOS 14 이상
- Xcode 16 이상
- Swift 6

```bash
git clone https://github.com/jureuk7/GeulGuard.git
cd GeulGuard
./scripts/test.sh
./scripts/build-app.sh
```

## 작업 브랜치와 Pull Request

새 작업은 `main`에서 분기한 `<종류>/<주제>` 브랜치에서 진행하고 PR로
병합합니다. 한 PR에는 하나의 목적과 그 목적에 필요한 테스트·문서를 담습니다.

- [브랜치 가이드](docs/branch-guide.md): 이름, 작업 시작, 워크트리 분리, 동기화와 정리
- [PR 가이드](docs/pr-guide.md): 제목·본문, 변경별 검증, 리뷰와 병합 기준
- [PR 템플릿](.github/pull_request_template.md): 새 PR 작성 양식

동작 변경에는 회귀 테스트 또는 기존 테스트의 보호 근거가 필요합니다.
사용자에게 보이는 변경에는 README 또는 `docs/` 문서를 함께 수정합니다.
문서만 변경한 경우 링크·절차·서식을 확인하고 로컬 앱 빌드 생략 이유를 남깁니다.
세부 검증은 PR 가이드의 변경별 기준을 따릅니다.

커밋 메시지는 변경 이유가 드러나는 짧은 명령형 문장을 권장합니다.

## 리뷰와 배포

리뷰어가 재현할 수 있도록 검증 방법과 결과를 적고, 피드백에 따라 같은 브랜치에
수정 커밋을 추가해 주세요. 병합과 릴리스는 메인테이너가 진행합니다.

배포용 인증서나 개인 키는 기여에 필요하지 않습니다. 서명·공증이 필요한 변경은
가능한 로컬 검증과 남은 항목을 PR에 적어 주세요. 배포 환경의 검증은 메인테이너가
담당합니다. 키, 토큰, 개인정보는 이슈·PR·소스에 포함하지 마세요.
